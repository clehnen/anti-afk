export interface ArgumentSettings {
  /**
   * Period without movement to wait befor starting the interval.
   */
  timeout: number;
  /**
   * Time to wait between movements
   */
  interval: number;
}
